{
  "buildCommand": "cd ../.. && pnpm build --filter=@sw/s_w_graphserver...",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/graphql",
      "destination": "/api/graphql"
    }
  ],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
